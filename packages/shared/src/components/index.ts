// Shared design system components (UI primitives + custom components)
// Re-export from this directory when components are moved from packages/lab

export const SharedComponents = {
  // TODO: Radix UI components will be tree-shaken per route
  // Wordmark, StatusPill, StatRow, Stepper, etc. will be exported here
}
